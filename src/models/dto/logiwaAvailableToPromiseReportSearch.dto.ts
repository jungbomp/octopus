export interface LogiwaAvailableToPromiseReportSearchDto {
    pageSize?: number; // Default is 200
    selectedPageIndex?: number; // Default is 1
    id?: number; // inventoryItemId
    code?: string; // stdSku
  }